server {
    listen 80;

    location /dashboard/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /dashboard/index.html;
    }

    location = / {
        return 301 /dashboard/;
    }
}
